import React from 'react';
import Collapse from 'components/Collapse';

export default {
  title: 'Collapse',
  component: Collapse,
};

export const Default = () => (
  <Collapse>
    <div>exepmle</div>
  </Collapse>
);
